"""Legacy reports module — keeps the v1 /api/v1/analyze PDF flow alive
while customers transition to the runbook generator at
/api/v3/projects/runbook (which is the strategically correct place for
deliverable PDFs).
"""
